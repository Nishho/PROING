import 'package:flutter/material.dart';

class QuitarRamo extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Eliminar Ramo'),
      ),
    );
  }
}