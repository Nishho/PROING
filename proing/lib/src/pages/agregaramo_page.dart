import 'package:flutter/material.dart';

class AgregaRamo extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Agregar Ramo'),
      ),
    );
  }
}